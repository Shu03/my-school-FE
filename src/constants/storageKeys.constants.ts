/**
 * Keys used for browser storage (localStorage / persisted stores).
 */
export const STORAGE_KEYS = {
    ACCESS_TOKEN: "accessToken",
    REFRESH_TOKEN: "refreshToken",
    AUTH_STORE: "auth-storage",
    THEME: "theme",
} as const;
